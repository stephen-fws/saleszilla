# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database (SQL Server)
sqlalchemy>=2.0.0
pyodbc>=5.1.0

# Auth
PyJWT>=2.8.0
python-multipart>=0.0.9

# Microsoft Graph API
httpx>=0.27.0

# Session middleware
itsdangerous>=2.2.0

# Validation & settings
pydantic[email]>=2.7.0
python-dotenv>=1.0.0

# Email body rendering (markdown -> HTML)
markdown>=3.5.0

# SendGrid (OTP emails)
sendgrid>=6.11.0

# Anthropic Claude API
anthropic>=0.40.0

# HTTP requests (agentflow triggers)
requests>=2.31.0

# Google Cloud Storage (file uploads)
google-cloud-storage>=2.14.0

# Chat file attachments — PDF and DOCX text extraction
pypdf>=4.0.0
python-docx>=1.1.0

# Twilio (browser-based calling via Client SDK)
twilio>=9.0.0

# IANA tz database for zoneinfo on Windows (Python's stdlib zoneinfo has no
# bundled data on Windows; this pure-Python package provides it). Used by the
# calendar route's MS Graph timezone conversion.
tzdata>=2024.1
